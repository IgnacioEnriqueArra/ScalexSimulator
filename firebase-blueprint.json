{
  "entities": {
    "Room": {
      "title": "Room",
      "description": "A game room connecting a host (screen) and a controller (mobile).",
      "type": "object",
      "properties": {
        "hostId": {
          "type": "string",
          "description": "The UID of the user who created the room."
        },
        "isAccelerating": {
          "type": "boolean",
          "description": "Whether the controller is currently pressing the gas."
        },
        "isReversing": {
          "type": "boolean",
          "description": "Whether the controller is currently pressing reverse."
        },
        "steering": {
          "type": "number",
          "description": "Steering value from -1 (left) to 1 (right)."
        },
        "controllerConnected": {
          "type": "boolean",
          "description": "Whether a controller has joined the room."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "When the room was created."
        }
      },
      "required": ["hostId", "isAccelerating", "isReversing", "steering", "controllerConnected", "createdAt"]
    }
  },
  "firestore": {
    "rooms/{roomId}": {
      "schema": {
        "$ref": "#/entities/Room"
      },
      "description": "Stores active game rooms. The roomId is a 4-digit code."
    }
  }
}
